description: "Loan Request V1"
type: object
properties:
  id:
    type: integer
    format: int32
    description: "Loan request identifier"
  userId:
    type: integer
    format: int32
    description: "User identifier"
  bookId:
    type: integer
    format: int32
    description: "Book identifier"
  requestDate:
    type: string
    format: date
    description: "Request date"
  approvalDate:
    type: string
    format: date
    description: "Approval date"
  status:
    type: integer
    format: int32
    description: "Loan request status"
  rejectionReason:
    type: string
    description: "Rejection reason"
